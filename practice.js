const fruits = ['banan', 'apple', 'mango', 'straberry']

fruits.forEach((value, index) => {
    console.log(value, index)
})

let numbers = [20, 20, 20]
let sum = 0;

numbers.forEach((number)=>{
  sum = number;
})
console.log("total:" , sum)
